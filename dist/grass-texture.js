// Optimized directly from the user-provided Hi3D GLB. The source GLB is ~63 MB / ~2M triangles,
// so the browser build uses a baked surface asset to preserve its look without importing the full mesh.
export const GRASS_LUSH_URL='./assets/grass/hi3d_grass.webp';
